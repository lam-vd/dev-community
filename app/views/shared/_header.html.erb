<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <%= link_to "Dev Community", root_path, class: "navbar-brand", style: "padding-left: 20px;" %>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#">Home</a>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "Profile", member_path(current_user), class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure you wanna sign out?" }, class: "nav-link active" %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Sign in", new_user_session_path, class: "nav-link active" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>